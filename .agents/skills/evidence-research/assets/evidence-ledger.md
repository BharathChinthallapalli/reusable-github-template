# Engineering evidence ledger

Decision and requested scope:
Research date and environment/version boundary:
Actual fetch tool name and capability source:
Official URL, fetch time, requested/resolved version, section read and fetch outcome:
Evidence reused from this task, with originating tool result:
Missing capability or blocked external-dependent decision:
Inventory method, pages/counts and known omissions:

| Source / revision / path | Coverage actually read | Observation | Evidence type | Transfer decision and reason | Affected artifact / validation |
| --- | --- | --- | --- | --- | --- |

Evidence types: inspected implementation, declared intent, reproduced behavior,
historical report, inference. Use a precise label for each claim.

Coverage totals:
- Inventoried:
- Overviews read:
- Selected source reviewed:
- Fully read within a declared file scope:
- Unavailable or excluded, with reason:

Conflicting evidence and how it was resolved:
Unanswered questions that could change the decision:
Implementation decisions adopted / deferred / rejected:
Attribution and copying constraints:
Commands actually run and observed results, if any:

Command contract: exact command/tool arguments; shell and working directory;
installed version; official syntax URL; prerequisites; expected side effects;
expected result; actual exit/output or explicitly not run. Never record secrets.
