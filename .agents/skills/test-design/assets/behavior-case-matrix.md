# Behavior case matrix

Changed contract and public seam:
Baseline revision / reported failing behavior:
Existing test command and prerequisites:

| Case / representative input | Expected observable result | Independent oracle | Why this case matters | Result |
| --- | --- | --- | --- | --- |

Select relevant case classes: normal use, smallest/largest accepted input,
rejected input, absent versus empty values, cleanup, repeated call, persisted
state, alternate transport or supported environment. Do not fill irrelevant rows.

How the test command reports a failed contract:
Fixture isolation and cleanup:
Consumer/artifact check, if applicable:
Required gates actually run:
Remaining coverage limit:
