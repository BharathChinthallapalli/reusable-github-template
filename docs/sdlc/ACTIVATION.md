# Activation and evidence findings

The local foundation is implemented; the requested end-to-end operating system
is **not fully activated or validated**. No missing source or integration is a
reason to weaken Guardian.

| Finding | Current result | Owner / next evidence |
| --- | --- | --- |
| Two-source reading exception | AnnexI draft and ISO clause list omitted with user authorization | Legal/compliance owner supplies sources later |
| Human acceptance / provenance | No trusted issuer; accepted/source metadata never enables dispatch | Product/platform: authenticated review API bound to exact revision |
| Native runtime gates | Existing Guardian inert fixtures pass; no native Windows/VSCode/CLI end-to-end here | Endpoint/SOC: installed-version matrix and timeout tests |
| Guardian approval/identity broker | Existing staged deployment prerequisites unchanged | Security/identity: protected issuer, named identity context and ACLs |
| Upstream v6 attestation | Independent-harness semantic precheck only; no signature/Merkle interoperability verifier or CI signing key | Security/platform: verify byte contract, isolated signer, OIDC/KeyVault key-only operation |
| Static governance in CI | Functions and tests run via existing test discovery; no authenticated head/base diff ingestion or protected required-check rollout | Repository admin: protected policy checkout and branch rules |
| Spec job / issue assignment | Prompt and local renderer ready; no model generation, issue writes, assignment or PR writer activated | Platform/product: versioned CLI, auth, isolated read-only generation and approved writer |
| Daily automations | Re-homed prompt contracts only; no scheduler or3/day accounting deployed | Platform: trusted scheduler/state and admission adapter |
| Git metrics / publication | Explicit-event calculator only; some metrics unavailable, no weekly publisher | Delivery analytics: Git/issue/ledger/cost adapters with minimisation |
| Full upstream governance port | Concepts/checks adapted; original schemas/agents/commands are not all shipped | Maintainer: staged source-compatible port and per-runtime validation |
| Five archetype delivery profiles | Design/tests/bands guidance present; no verified Bicep module interface, what-if or cloud execution | Azure owner: select subscription, verify pinned AVM params and OIDC claims |
| Cross-runtime agent profiles / new skills | Six new skills statically validated; existing runtime profiles preserved; role prompts are not installed runtime configurations | Maintainer: reviewed profile integration without added tools |
| Full compliance evidence | Matrix maps control support, not proof of satisfaction; records mostly templates | System owner: scoped assessment, evidence, review and retention decisions |
| Synthetic PR/merge metrics | Fixture events only; no GitHub PR was created or merged for this example | Product/repository owner: separately authorized real loop |
| Append-only SOC evidence | Existing local Guardian ledger is mutable absent managed protection/collector | SOC: protected collector, ACLs, retention and clock synchronization |
| Revert-check / application evals / IaC policy | Existing tests preserved; project-specific production gates not added by this foundation | Project technical lead: accepted follow-on tasks |

## Safe activation order

1. Review this diff and its design binding. Confirm the intended project and owners.
2. Configure managed OS isolation, identity and existing Guardian outside worker control.
3. Install trusted CI validators and reviewer/acceptance provenance verification.
4. Validate an inert denied-command corpus and benign canary in each installed runtime.
5. Add read-only spec/review execution with explicit tools; missing capability means BLOCKED.
6. Authorize the separate issue/PR writer, then limited task dispatch and budget accounting.
7. Enable release only after independent review evidence, environment approval, rollback
   and an actual deployment authorization path have been demonstrated.

Never run live credential, persistence, Defender-tampering or exfiltration payloads
to prove a denial. Use inert parser fixtures and approved harmless canaries.
