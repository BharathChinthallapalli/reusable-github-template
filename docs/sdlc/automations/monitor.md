# Monitor — deterministic Maintain intake

The coordinator may run scripts/monitor.py with explicit project samples through
existing authorized tooling. No model decides threshold arithmetic. Validate
freshness, finite numbers, known metrics and calibrated positive variance.

Use evidence IDs for idempotency.1σ logs;2σ requests read-only diagnosis;3σ
proposes an intent PR. The local output is a proposal, not a complete accepted
intent or a runbook authorization. A trusted writer supplies missing intake
fields and enforces deduplication,3/day caps where applicable and human triage.

No shell/Graph/Azure remediation tools are exposed by this prompt. Invalid
samples, unknown modes, Guardian denial, pivots or timeout fail-open escalate.
