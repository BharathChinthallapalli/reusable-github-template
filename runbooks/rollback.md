# Rollback runbook

Before release, identify exact current/previous code, model, prompt, data/schema
and infrastructure versions; compatibility limits; authorized operator; trigger;
bounded command plan; expected observations; stop condition and recovery test.

Record a rehearsal in the approved environment. Do not assume a database
migration is reversible or roll back policy/logging controls. These instructions
are not a pre-approved executable runbook; activation needs authenticated approval.
